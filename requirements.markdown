# Required Module Installation

Install all required modules using the following commands:

```bash
pip install opencv-python
pip install pyautogui
pip install numpy
pip install pywin32
```

Or install all modules at once:

```bash
pip install opencv-python pyautogui numpy pywin32
```
